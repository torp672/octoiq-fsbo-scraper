requests==2.31.0
beautifulsoup4==4.12.2
firebase-admin==6.4.0
google-cloud-firestore==2.16.0
flask==3.0.0
gunicorn==21.2.0
lxml==4.9.3
# Removed Selenium and WebDriver (not needed for cost-effective version)
# Removed expensive API integrations
# Cost-effective version uses only requests + beautifulsoup